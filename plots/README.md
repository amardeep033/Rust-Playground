# plots

Plotting examples using Plotters for image output and Textplots for terminal charts.

## What it shows

- Generating a PNG chart
- Rendering a simple histogram in the terminal

## Run

```bash
cargo run
```

This creates `output.png` in the project folder.