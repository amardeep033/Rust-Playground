# criterion

Small placeholder crate for benchmarking experiments.

## Current state

The current `main.rs` is minimal and can be used as a starting point for adding Criterion benchmarks or timing experiments.

## Run

```bash
cargo run
```