# webhook-listner

Basic Actix Web webhook receiver that prints headers and request body for inspection.

## What it shows

- A single POST webhook endpoint
- Simple request debugging output

## Run

```bash
cargo run
```

Default route:

- `POST /webhook`