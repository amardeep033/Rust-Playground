# sorting

Large in-memory sorting experiment over randomly generated numeric records.

## What it shows

- Building a large vector of tuples
- Sorting with `sort_unstable_by`
- Range-style queries with `partition_point`
- Rough memory footprint reporting

## Run

```bash
cargo run --release
```